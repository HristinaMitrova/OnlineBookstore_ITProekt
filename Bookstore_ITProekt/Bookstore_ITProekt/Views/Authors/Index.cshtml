@model IEnumerable<Bookstore_ITProekt.Models.Author>

@{
    ViewBag.Title = "Index";
}

<h2 class="badge bg-warning" >List of Authors</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered">
    <tr style="background-color:darkgoldenrod">
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gender)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImgUrl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Biography)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gender)
        </td>
        <td>
            <img src="@item.ImgUrl" style="height:100px;width:100px" />
            <!--@Html.DisplayFor(modelItem => item.ImgUrl)-->
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Biography)
        </td>
        <td>
            @if (User.IsInRole("Admin"))
            {
                @Html.ActionLink("Delete", "Delete", new { id = item.Key })
                @Html.ActionLink("Details", "Details", new { id = item.Key })
                @Html.ActionLink("Edit", "Edit", new { id = item.Key })

            }
            else if (User.IsInRole("User"))
            {
                @Html.ActionLink("Details", "Details", new { id = item.Key })
            }
            

        </td>
    </tr>
}

</table>
